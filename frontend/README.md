Front-end skeleton (React + Vite).

This folder is a minimal starting point. To create a full app, run:

```bash
cd frontend
npm install
npm run dev
```

Design tokens: use bright blue (#1565FF) and neon yellow (#E6FF00) to match provided mockups.
